package com.cangyao;
public class SnakeGame {
    public static void main(String[] args) throws Exception {
      
      new GameFrame(); // without attaching it to a variable, it will still create the new instance, and if we don't plan on using the instance variable name `gameFrame`, we can just run new GameFrame.

    }
}
